<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.example.jawahir.mymovie_p0.MainActivity">
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <GridView
            android:id="@+id/gv_movie"
            android:numColumns="auto_fit"
            android:gravity="center"
            android:columnWidth="@dimen/layout_len_extra_large"
            android:stretchMode="columnWidth"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent">
        </GridView>
        <ProgressBar
            android:id="@+id/pb_movie"
            android:layout_width="@dimen/layout_len_small"
            android:layout_height="@dimen/layout_len_small"
            android:visibility="invisible"
            android:layout_gravity="center"/>
        <TextView
            android:id="@+id/tv_error_msg"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="invisible"
            android:text="@string/error_id"
            android:textSize="@dimen/txt_size_extra_large"/>
    </FrameLayout>

</LinearLayout>
